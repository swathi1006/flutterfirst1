import 'package:flutter/material.dart';

class HomeSQFLIte extends StatelessWidget {
  const HomeSQFLIte({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Welcome!",style: TextStyle(fontSize: 30),),
      ),
    );
  }
}
